import { ApostrophePipe } from './apostrophe.pipe';

describe('ApostrophePipe', () => {
  it('create an instance', () => {
    const pipe = new ApostrophePipe();
    expect(pipe).toBeTruthy();
  });
});
